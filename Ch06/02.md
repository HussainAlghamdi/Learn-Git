# <div dir = rtl >  إنشاء و عرض الفروع |  Git Branches </dir >


### <div dir = rtl > لإستعراض الفروع نكتب :</dir >
```shell
git branch
```
### <div dir = rtl > لإنشاء فرع جديد نكتب : </dir >
```shell
git branch NamOfBranch
```
