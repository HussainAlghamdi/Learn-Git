# <div dir = rtl >الأمر | Git add</dir >

### <div dir = rtl >  ينقل الملفات من مرحله Untracked  الى مرحله Staged :</dir >
#### <div dir = rtl > لإضافة ملف واحد فقط نكتب </dir >

```shell
git add NameFile
```
#### <div dir = rtl > لإضافة كل الملفات في وقت واحد نكتب </dir >
```shell
git add .
```
