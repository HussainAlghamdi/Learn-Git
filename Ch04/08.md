# <div dir = rtl >  إخفاء الملفات باستخدام gitignore</dir >


### <div dir = rtl > ننشئ الملف التالي :</dir >
```shell
touch .gitignore
```

### <div dir = rtl > ندخل الملفات داخل gitignore. :</dir >
```shell
echo NameOfFile > .girignore
```

